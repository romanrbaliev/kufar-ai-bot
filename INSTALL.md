
# Руководство по установке 🚀



## ⚡️ Вариант для профи

1. `cp .env.example .env` -> настройте `N8N_WEBHOOK_URL`.

2. `pip install -r requirements.txt` && `python3 -m playwright install chromium`.

3. `python3 -m hh_automation.services.login` (создайте `data/session.json`).

4. `docker-compose up -d`.


---


## 🛠 Вариант для новичков (Пошагово)



### Шаг 1: Подготовка (Python и Docker)

1. **Python**: Откройте терминал, введите `python3 --version`. Если не работает — скачайте с [python.org](https://www.python.org).

2. **Docker**: Скачайте и запустите [Docker Desktop](https://www.docker.com). Убедитесь, что значок кита в панели стал зеленым.



### Шаг 2: Настройка настроек

1. Переименуйте файл `.env.example` в `.env`.

2. Откройте его и вставьте адрес вашего вебхука из n8n (Production URL).



### Шаг 3: Вход в аккаунт (Сессия)

1. В терминале введите: `pip3 install -r requirements.txt`.

2. Введите: `python3 -m playwright install chromium`.

3. Запустите: `python3 -m hh_automation.services.login`.

4. В открывшемся окне войдите на Kufar. Увидев свои чаты, вернитесь в терминал и нажмите **Enter**.



### Шаг 4: Запуск

Введите финальную команду:

`docker-compose up -d`



### Шаг 5: Обучение «мозга» (Импорт схемы в n8n)

Теперь нужно загрузить готовую логику ответов в n8n.

1. Откройте в браузере: http://localhost:5678.

2. Если n8n открылся впервые, пройдите простую регистрацию (создайте аккаунт).

3. В центре экрана или в меню выберите Import from File (Импорт из файла).

4. Выберите файл Kufar_Full_Automation.json из папки вашего проекта.

5. Настройка нейросети:

- Найдите узел с названием OpenRouter.

- Нажмите на него и в поле Credentials создайте новое подключение, вставив ваш API ключ от OpenRouter.

6. Финальный пуск: В правом верхнем углу нажмите кнопку Publish (Опубликовать).

Важно: Только после нажатия кнопки Publish система начнет принимать боевые сообщения.


### Шаг 6: Управление через браузер

1. Зайдите на http://localhost:9000 (Portainer).

2. Задайте пароль администратора при первом входе.

3. В разделе Containers нажмите иконку Logs у kufar-bot, чтобы видеть работу робота в реальном времени.

